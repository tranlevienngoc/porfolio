import { Flex } from '@chakra-ui/react';

const Settings = () => {
  return (
    <Flex className='settings-page' width='100%' height='100%'>
      Settings
    </Flex>
  );
};

export default Settings;
